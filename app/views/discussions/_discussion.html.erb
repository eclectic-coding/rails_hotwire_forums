<div id="<%= dom_id(discussion) %>" class="mb-2">
  <h2><%= link_to discussion.name, discussion_path(discussion) %></h2>
  Pinned: <%= discussion.pinned %> | Closed: <%= discussion.closed %><br />
  Posted in: <%= discussion.category_name %> | Posts: <%= discussion.posts_count %><br />
  Posted by: <%=  discussion.user.username.capitalize %>
  <div>
    <%= link_to "Edit", edit_discussion_path(discussion) %> |
    <%= link_to "Delete", discussion, method: :delete, data: { confirm: "Are you sure?" } %>
  </div>
</div>